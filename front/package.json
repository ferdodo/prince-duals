{
  "scripts": {
    "build": "tsc && vite build",
    "start": "vite --host --port 3366"
  },
  "devDependencies": {
    "core": "file:../core",
    "rxjs": "^7.8.1",
    "typescript": "^5.0.4",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "connection-types": "^1.0.0"
  }
}
